export interface Match {
  id: number;
  home_name: string;
  away_name: string;
  begin_date: Date;
  date: Date;
  is_current: boolean;
  match_id: string;
  round: number;
  is_complate: boolean;
}
